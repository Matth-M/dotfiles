# ls
alias ls="ls --color=auto"
alias ll="ls -lF"
alias lla="ls -AlF"
alias lal="ls -AlF"
alias la="ls -A"
alias l="ls -CF"
alias lg="ls -A | rg"

# rm
alias rm="rm -I"
alias trash="mv -f -t $HOME/Trash"

alias pacman="pacman --color=auto"

# Grep
alias grep="grep --color=auto"
alias fgrep="fgrep --color=auto"
alias egrep="egrep --color=auto"

# Dotfiles
alias dots="/usr/bin/git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME" 

# Useful commands
alias b="sudo brightnessctl s"
alias s="pactl set-sink-volume @DEFAULT_SINK@"
alias maj="sudo dnf --assumeyes update && sudo dnf --assumeyes upgrade && sudo dnf --assumeyes	autoremove"
alias copy="xclip -sel c <"
alias set-fan="sudo ectool --interface=lpc fanduty"
alias auto-fan="sudo ectool --interface=lpc autofanctrl"
alias reload="source ~/.zshrc && source ~/.zprofile"
alias nr="nohup redshift >/dev/null 2>&1 &"
alias kr="killall redshift; redshift -x"
alias ng="nohup gammastep >/dev/null 2>&1 &"
alias kg="killall gammastep" # ; gammastep -x"
alias refresh-wifi="sudo systemctl restart doh-client.service NetworkManager.service"

# Paths
alias cours="cd ~/Documents/ISMIN/3A"
alias wiz="cd ~/Documents/ISMIN/Cours_ISMIN/PI/code/"
alias gmd="cd ~/Documents/ISMIN/3A/MD"
alias gd="cd ~/dev"
alias gn="cd $HOME/.config/nvim"
alias gc="cd $HOME/.config"
alias gt="cd $HOME/test"

# Bluetooth
# SONY_HEADPHONES_ID="94:DB:56:AB:52:8E"
# SPEAKER="D8:37:3B:D0:48:B2"
# EARBUDS="50:C2:75:30:03:40"
alias sony3="bluetoothctl disconnect 94:DB:56:AB:52:8E; bluetoothctl connect 94:DB:56:AB:52:8E"
alias sony="bluetoothctl disconnect 88:C9:E8:6A:60:41; bluetoothctl connect 88:C9:E8:6A:60:41"
alias speaker="bluetoothctl disconnect D8:37:3B:D0:48:B2; bluetoothctl connect D8:37:3B:D0:48:B2"
alias earbuds="bluetoothctl disconnect E8:C9:13:BC:61:A4; bluetoothctl connect E8:C9:13:BC:61:A4"
alias corsair="bluetoothctl disconnect B4:84:D5:70:E2:39; bluetoothctl connect B4:84:D5:70:E2:39"
alias btd="bluetoothctl disconnect"
alias bt="bluetoothctl"

# Neovim/Vim
alias v="vim"
alias nv="nvim"
alias nvl="NVIM_APPNAME=lazy_nvim nvim"
alias nvp="NVIM_APPNAME=nvim_packer nvim"
alias nvv="NVIM_APPNAME=nvim_vanilla nvim"
alias nvc="NVIM_APPNAME=nvchad nvim"

# Tmux
alias t="tmux"
alias zj="zellij"

# Programs
alias matlab="bash ~/matlab/bin/matlab"
alias py="python3"
alias pn="pnpm"



# https://gist.github.com/rajeshg/712300
#   mkcd command
#   This is an improvised version of the mkcd command at http://superuser.com/questions/152794/is-there-a-shortcut-to-mkdir-foo-and-immediately-cd-into-it
#   This function has to be added to the ~/.bashrc file
#   After that you can run command like: mkdir abc, mkdir -p one/two/three
#
function mkcd {
  last=$(eval "echo \$$#")
  if [ ! -n "$last" ]; then
    echo "Enter a directory name"
  elif [ -d $last ]; then
    echo "\`$last' already exists"
  else
    mkdir $@ && cd $last
  fi
}
